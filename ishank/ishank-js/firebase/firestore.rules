rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /meta/{docId} {
      allow read: if true;
      allow create, update: if docId == "app";
      allow delete: if false;
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}
